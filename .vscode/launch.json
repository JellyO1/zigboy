{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Debug Main",
			"type": "lldb",
			"request": "launch",
			"program": "${workspaceFolder}/zig-out/bin/zig_gbc",
			"preLaunchTask": "build",
			"cwd": "${workspaceFolder}"
		}
	]
}